import type { APIListResponse } from '../../../models/base'
import type { Set } from '../../../models/set'

export const setInventorySets: APIListResponse<Set> = {
  count: 0,
  next: null,
  previous: null,
  results: []
}
